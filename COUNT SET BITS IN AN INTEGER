//  COUNT SET BITS IN AN INTEGER

#include<iostream>

using namespace std;

int main(){
    int a,div,c=0;
    cout<<"Enter the integer:";
    cin>>a;
    while(a>0)
    {
        if(a&1)
        c++;
        a=a>>1;
    }
cout<<"SET BITS IN A GIVEN  INTEGER IS "<<c;
return 0;
}
